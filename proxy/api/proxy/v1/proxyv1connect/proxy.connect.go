// Package api.proxy.v1 contains the protobuf definitions for the ISA
// platform Proxy service (services/proxy). The proxy fronts outbound
// integrations on behalf of authenticated callers: it verifies an
// inbound HMAC signature, looks up the target integration by scope,
// applies the per-integration host allowlist, and forwards the call.
//
// proxy.proto declares the gRPC surface (ProxyService.Call) and the
// request / response messages. The wire format intentionally mirrors
// the existing JSON envelope served by services/proxy so the gRPC
// front door can be enabled without a behavioural change.

// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: api/proxy/v1/proxy.proto

package proxyv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/isa-sdk/sdk/proxy/api/proxy/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// ProxyServiceName is the fully-qualified name of the ProxyService service.
	ProxyServiceName = "api.proxy.v1.ProxyService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// ProxyServiceCallProcedure is the fully-qualified name of the ProxyService's Call RPC.
	ProxyServiceCallProcedure = "/api.proxy.v1.ProxyService/Call"
)

// These variables are the protoreflect.Descriptor objects for the RPCs defined in this package.
var (
	proxyServiceServiceDescriptor    = v1.File_api_proxy_v1_proxy_proto.Services().ByName("ProxyService")
	proxyServiceCallMethodDescriptor = proxyServiceServiceDescriptor.Methods().ByName("Call")
)

// ProxyServiceClient is a client for the api.proxy.v1.ProxyService service.
type ProxyServiceClient interface {
	// Call forwards the request body to the integration identified by
	// `target` and returns the upstream response verbatim. The proxy
	// enforces the per-integration host allowlist and SSRF guard before
	// dialing the upstream.
	Call(context.Context, *connect.Request[v1.CallRequest]) (*connect.Response[v1.CallResponse], error)
}

// NewProxyServiceClient constructs a client for the api.proxy.v1.ProxyService service. By default,
// it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and
// sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC()
// or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewProxyServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) ProxyServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	return &proxyServiceClient{
		call: connect.NewClient[v1.CallRequest, v1.CallResponse](
			httpClient,
			baseURL+ProxyServiceCallProcedure,
			connect.WithSchema(proxyServiceCallMethodDescriptor),
			connect.WithClientOptions(opts...),
		),
	}
}

// proxyServiceClient implements ProxyServiceClient.
type proxyServiceClient struct {
	call *connect.Client[v1.CallRequest, v1.CallResponse]
}

// Call calls api.proxy.v1.ProxyService.Call.
func (c *proxyServiceClient) Call(ctx context.Context, req *connect.Request[v1.CallRequest]) (*connect.Response[v1.CallResponse], error) {
	return c.call.CallUnary(ctx, req)
}

// ProxyServiceHandler is an implementation of the api.proxy.v1.ProxyService service.
type ProxyServiceHandler interface {
	// Call forwards the request body to the integration identified by
	// `target` and returns the upstream response verbatim. The proxy
	// enforces the per-integration host allowlist and SSRF guard before
	// dialing the upstream.
	Call(context.Context, *connect.Request[v1.CallRequest]) (*connect.Response[v1.CallResponse], error)
}

// NewProxyServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewProxyServiceHandler(svc ProxyServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	proxyServiceCallHandler := connect.NewUnaryHandler(
		ProxyServiceCallProcedure,
		svc.Call,
		connect.WithSchema(proxyServiceCallMethodDescriptor),
		connect.WithHandlerOptions(opts...),
	)
	return "/api.proxy.v1.ProxyService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case ProxyServiceCallProcedure:
			proxyServiceCallHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedProxyServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedProxyServiceHandler struct{}

func (UnimplementedProxyServiceHandler) Call(context.Context, *connect.Request[v1.CallRequest]) (*connect.Response[v1.CallResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("api.proxy.v1.ProxyService.Call is not implemented"))
}
